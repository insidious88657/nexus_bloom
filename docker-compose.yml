services:
  server:
    build: ./server
    ports:
      - "${SERVER_PORT:-3000}:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env
    command: npm run dev

  # Add other services as needed
